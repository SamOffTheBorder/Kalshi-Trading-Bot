## 1. Strategy registry (unblocks everything else)

Ordered first because nothing downstream is testable while `--strategy-id` is
a label that selects nothing.

- [x] 1.1 Add `strategy/registry.py`: a frozen mapping from `strategy_id` to a constructed strategy, its frozen config, and its declared gate status (`passed` | `gate_failed` | `never_gated` | `parked`).
- [x] 1.2 Register all seven strategies with the statuses recorded in design.md D4, each citing the task that established it. (Six entries: `funding_carry` is a two-leg function, not a `StrategyProtocol` class — it is registered on the perp side in §5, not here.)
- [x] 1.3 Refuse an unknown `strategy_id` at preflight with reason `unknown_strategy_id`, before any run row is written or market data fetched. (`run_preflight` resolves and validates the id immediately after the paper guard, before asset admission; `resolve_strategy_id` maps the legacy `"unspecified"`/`""` sentinels to the `hold` default.)
- [x] 1.4 Persist `strategy_id`, config version, and gate status on `PaperRun`; additive nullable columns only, with a `SCHEMA_VERSION` bump. (Three nullable columns on `PaperRun`; `SCHEMA_VERSION` 13 → 14; auto-added by `storage/db._add_missing_columns`. Also threaded into the `preflight_passed` audit payload.)
- [x] 1.5 Add tests: known id constructs the right strategy; unknown id fails preflight with no run row; default is the hold strategy; a paper result does not mutate gate status. (`tests/unit/test_strategy_registry.py`, 11 tests.)

## 2. Strategy context assembly

- [x] 2.1 Add a `StrategyContext` builder to `PredictionPaperAdapter`, populating market identity, clocks, quotes, spot, vol, trend z-score, and `spot_bars`. (`StrategyContextBuilder` in `prediction_adapter.py`; loads spot close/OHLCV + BRTI once per asset, slices per evaluation with a bisect. `PredictionQuote` gained `strike_type`/`floor_strike`/`cap_strike`, populated by `kalshi_public_quote_source`.)
- [x] 2.2 Filter every input on `available_at <= now_ts`, matching `BacktestEngine._bars_before`'s discipline; never filter on `observed_at`. (BRTI sliced on `usable_at` = `available_at`; spot bars on `open_ts < now_ts`; spot/vol on the latest close at-or-before `now_ts` — same recipe as the engine.)
- [x] 2.3 Leave a field unset where its input is unavailable; never substitute a default or forward-fill. (Missing symbol → `spot=0.0`, `vol_source="unavailable"`, `trend_zscore=None`, empty `spot_bars`; the strategy's own guards handle the sentinels, as in the engine.)
- [x] 2.4 Adapt `StrategyProtocol.evaluate` results onto the adapter's existing fill path, preserving the `may_fill` contract. (`_decision_to_signal` maps HOLD → hold, BUY_YES/BUY_NO → buy at the decision's own executable price; the rest of `evaluate()` — would-fill, `may_fill` gate, `place_order` — is unchanged.)
- [x] 2.5 Retain `StrategyFn` only where the sports domain uses it; remove its use from the prediction path. (`_is_strategy_protocol` dispatches: a `StrategyProtocol` object gets the context path, a bare callable gets the legacy quote path. Sports keeps its own `SportsStrategyFn`; the prediction default is still `hold_strategy` for callers that pass nothing, but `run_paper.py` now builds a registry strategy — see §2 CLI wiring below.)
- [x] 2.6 Add tests: a bar with `available_at > now_ts` is excluded; insufficient history yields `trend_zscore=None`; a BUY decision produces a fill when `may_fill`, and a recorded decision without a fill when not. (`tests/unit/test_prediction_context_builder.py`, 8 tests — BRTI slice on `usable_at`, spot-bar exclusion at `now_ts`, sentinel spot/`None` trend, fill vs shadow-no-fill.) Also wired `scripts/run_paper.py:_build_adapter` to construct the registry strategy via `build_strategy(resolve_strategy_id(...))` instead of the old `hold_strategy` stub.

## 3. Reconstructed-data boundary regression

- [x] 3.1 Add a test proving a BUY decision from a real strategy on a `reconstructed`-backed run records the decision and creates no fill, with reason `reconstructed_data_not_admissible`. (`test_buy_decision_records_no_fill_for_reconstructed_manifest` — `_AlwaysBuy` strategy, `manifest_provenance=lambda _a: "reconstructed"`, zero `SimulatedTrade` rows.)
- [x] 3.2 Add a test proving the same run with a `source_native` manifest does fill — so the refusal is attributable to provenance, not to the strategy failing to signal. (`test_source_native_manifest_lets_the_same_decision_fill` — same strategy, `manifest_provenance=lambda _a: "source_native"`, one fill.)

## 4. Perp paper adapter

<!-- §4-§5 are Opus-recommended in proposal.md's Model complexity table
     (two-leg coordination, funding, bracket/liquidation interaction). -->


- [x] 4.1 Add `execution/perp_adapter.py` assembling `perp_paper` and `perp_admission` into a run loop matching `PredictionPaperAdapter`'s preflight/evaluate/reconcile shape. (`PerpPaperDomainAdapter` — `evaluate`/`reconcile` over injected read-only `PerpMarketReader`/`PerpCoverageReader`/`PerpDiscoveryReader`; delegates fills/brackets/liquidation/reconciliation to `execution/perp_paper.PerpPaperAdapter`. DB-backed reader factories `db_perp_*` read captured `PerpMarkObservation`/`PerpFundingObservation`/`DiscoveryResult` rows, causally on `available_at`.)
- [x] 4.2 Wire `--domain perp` in `scripts/run_paper.py`, replacing the `SystemExit`. (`run_id` now generated in the CLI and shared with both the perp adapter — which persists `PerpPaperPosition.paper_run_id` — and the orchestrator. Directional strategy defaults to `hold_perp_strategy`: the registry holds only single-instrument `StrategyProtocol` strategies; a directional perp strategy is out of this change's scope.)
- [x] 4.3 Gate fills on `evaluate_perp_admission`; an asset failing admission still records decisions, with the refusal reason persisted. (`admission_may_fill = verdict.admitted and verdict.may_fill and may_fill`; the reason string lands in the decision payload as `admission_reason`.)
- [x] 4.4 Require a `validate_bracket`-accepted stop and target on every entry; refuse entry otherwise with no position row created. (Adapter calls `validate_bracket` before `pa.open`; a missing/degenerate bracket → `invalid_signal`, a directionally-wrong one → `invalid_bracket` with the `PerpPaperError` message, both before any `PerpPaperPosition` row.)
- [x] 4.5 Apply funding as a distinct event type, keeping `funding_pnl_usd` separate from `realized_pnl_usd`. (`apply_funding()` accrues `realized_funding(...)` into `funding_pnl_usd` and writes an `event_type="funding"` `PerpPaperEvent`; `realized_pnl_usd` is untouched.)
- [x] 4.6 Assert no execution client is constructed and no order endpoint is called on any perp path. (Adapter takes only plain callables + a `Session`; `test_no_execution_client_is_constructed` asserts no field exposes `place_order`/`create_order`.)
- [x] 4.7 Add tests: full lifecycle; admission refusal; long stop-out; invalid bracket refused; funding applied without touching realized PnL. (`tests/unit/test_perp_adapter.py`, 12 tests. Note: intrabar stop-out is `PerpPaperAdapter.step`'s job, already covered by `test_perp_paper*.py`; this adapter's tests cover the enter/refuse/funding surface it owns.)

## 5. Two-leg funding carry

- [x] 5.1 Add a two-leg decision path to the perp adapter; do not widen `Decision`. (`PerpPaperDomainAdapter.evaluate_carry()` drives `strategy/funding_carry.evaluate_funding_carry`; `Decision` is untouched — the perp leg goes through `PerpPaperAdapter.open`, the hedge leg is its own `PerpPaperEvent`.)
- [x] 5.2 Record both legs' costs so the result reads as net carry after fees rather than as a win rate. (`event_type="hedge_leg"` payload carries `hedge_cost_usd`, `expected_funding_usd`, `expected_net_carry_usd`, and `perp_entry_fee_usd`.)
- [x] 5.3 Refuse a carry entry whose funding does not cover both legs' transaction costs, recording the refusal reason. (`should_enter=False` → `status="carry_not_entered"` with `reason` = `funding_rate_too_small` / `net_carry_below_gate` / `hedge_not_linear_carry_disabled`, and the carry numbers in the payload.)
- [x] 5.4 Add tests: both legs recorded with expected net carry; sub-cost funding refused; the `funding_carry.py` 50¢-hedge fee-drag case produces a refusal at a realistic funding rate. (In `test_perp_adapter.py`: `test_carry_fills_both_legs_...` (eligible linear hedge, fill path), `test_carry_refused_when_funding_below_min_rate`, `test_carry_50c_hedge_fee_drag_refuses_at_a_realistic_rate` (0.03% rate → negative net carry → refused).)

## 6. Multi-account launcher

- [x] 6.1 Add `scripts/run_strategy_lab.py`: start N concurrent paper runs from a declarative spec (strategy, config, assets, bankroll, mode, duration per account). (JSON spec → `AccountSpec` list; each account is a full `PaperOrchestrator` run in its own daemon thread with its own `run_id`.)
- [x] 6.2 Give each account its own bankroll and capital ledger; sizing must never observe another account's cash, positions, or equity. (Each adapter builds its own `PaperBroker(starting_cash_usd=spec.bankroll_usd)` / `PerpPaperDomainAdapter(collateral_usd=...)`; nothing is shared across accounts. `test_each_account_broker_is_seeded_with_its_own_bankroll` asserts distinct brokers with distinct cash.)
- [x] 6.3 Bound the account count and document a per-run poll interval, so N accounts do not defeat the existing 429 backoff. (`MAX_ACCOUNTS = 8`; `poll_interval_seconds` raised to a `MIN_POLL_INTERVAL_SECONDS = 10.0` floor with a warning; documented in the module docstring.)
- [x] 6.4 Handle SIGINT/SIGTERM across all accounts, persisting a final heartbeat and reconciliation status for each. (One Ctrl+C in the supervisor calls `orchestrator.request_stop()` — a new public seam on `PaperOrchestrator` — on every account, then joins each thread; each orchestrator's own `finally` writes its final heartbeat + `report` audit + ended status.)
- [x] 6.5 Keep it foreground-only — no scheduler, no daemon, no auto-restart, per the standing O1 decision. (Documented in the module docstring; the supervisor loop just waits on threads and exits.)
- [x] 6.6 Add tests: two concurrent runs record against distinct `paper_run_id`s; one run's halt does not halt another; per-account sizing is isolated. (`tests/unit/test_strategy_lab_launcher.py`, 9 tests. The orchestrator's `_cycle` already absorbs a faulting adapter cycle-by-cycle, so the isolation guarantee is stronger than required — a crashing account doesn't even stop itself, let alone a sibling.)

## 7. Dashboard sections

- [x] 7.1 Add a perpetuals section: open positions, brackets, funding accrued, liquidation distance, admission status. (`perp_positions_view()` + a "Perpetual paper positions" panel on `/perps` (`market_area.html`), shown only when the domain has perp positions. `PerpPositionView.liquidation_distance_pct` is side-aware.)
- [x] 7.2 Add a 15-minute scalping section: per-run decisions, fills, win rate against the applicable cost floor, and current blockers. (The scalping runs are prediction-domain paper runs, already listed per-domain on `/prediction-markets` with the enhanced row — strategy, gate, decisions/fills, blockers. The `/strategy-lab` comparison table adds net + flat-sizing PnL across all runs. A per-run realized win rate vs the cost floor needs `SimulatedTrade` rows attributable to a `paper_run_id`, which the schema does not carry — noted as a follow-up rather than faked.)
- [x] 7.3 Add a side-by-side comparison view keyed by `paper_run_id`, showing strategy, gate status, PnL, and flat-sizing PnL together. (`strategy_lab_comparison()` + the new `/strategy-lab` route and `strategy_lab.html`; `LabAccountComparison` rows keyed by `run_id`.)
- [x] 7.4 Surface gate status prominently on every run row; a `gate_failed` or `never_gated` run must be legible as such at a glance. (Gate column with a coloured badge on the `index.html` overview table, the `market_area.html` per-domain table, and the `/strategy-lab` comparison.)
- [x] 7.5 Surface a decisions-without-fills run (awaiting captured BRTI, or reconstructed-backed) as an explained state rather than an apparent malfunction. (`PaperRunSummary.decisions_without_fills` — decisions > 0, fills == 0, every admitted asset blocked — renders a "recording only" / "recording, not filling" badge plus the per-asset reason, not a health warning.)
- [x] 7.6 Add tests for each new query and panel, following `test_dashboard_capture_panels.py`'s shape. (`tests/unit/test_dashboard_strategy_lab.py`, 9 tests, including a `/strategy-lab` route render.)

## 8. Documentation and verification

- [x] 8.1 Document the registry, its gate statuses, and their basis in `docs/paper-operations.md`. (New "Strategy lab and multiple paper accounts" section with the full gate-status table and the `unknown_strategy_id` preflight behaviour.)
- [x] 8.2 Document why the lab has no backtest mode (design D3: the holdout is spent), so the question is not re-litigated. (Subsection "Why the lab has no backtest mode" in the same doc, plus a panel on the `/strategy-lab` page itself.)
- [x] 8.3 Document the per-account isolation rule and its origin in §8.3's shared-ledger sizing bug. (Subsection "Per-account isolation" — names the v2 §8.3 phantom-drawdown bug as the reason, plus `MAX_ACCOUNTS` and the poll-interval floor.)
- [x] 8.4 Run `uv run pytest` and `uv run ruff check`; confirm the full suite passes. (861 passed, 3 deselected. All files changed by this change are ruff-clean; the 10 remaining ruff errors are pre-existing in untouched files — sports module, kalshi client, `capture_status.py`, `export.py`, `test_export_redaction.py` — and are unchanged by this work.)
- [x] 8.5 Verify against a real bounded paper session: start two concurrent accounts with different strategies, confirm isolation, gate labelling, and that the reconstructed boundary refuses fills where it applies. Record the honest result.
      **Done.** `scripts/run_strategy_lab.py` with two prediction accounts against a scratch DB started both with **distinct run ids**, isolated (one account's preflight refusal did not affect the other's startup or shutdown), and forwarded a clean stop. Against the **committed** crypto registry both accounts are correctly refused with `BTC=lifecycle_backtest_no_decisions` — BTC's 15m instrument is not promoted past `backtest` lifecycle, so it is not paper-admissible; that gate is working as designed, not a bug in this change.
      A second end-to-end run with ETH temporarily promoted to `paper` lifecycle exercised the full path: account A (`trend_scalp`, `gate_failed`, **source-native** manifest) → `decision status=filled` + a `fill` audit event; account B (`settlement_prob`, `never_gated`, **reconstructed** manifest) → `decision status=shadow_would_fill / reason=shadow_mode_no_paper_fill` on every cycle, **no fill**. Gate status was persisted on each `PaperRun` (`gate_failed` / `never_gated`) and the reconstructed manifest downgraded `may_fill` at preflight exactly as the boundary requires. The honest result at this stage: the lab, the registry, gate labelling, per-account isolation, and the reject-but-never-admit boundary all work; real paper trading of the crypto 15m series still waits on an operator promoting an asset's lifecycle past `backtest`, which is deliberately not this change's call.
